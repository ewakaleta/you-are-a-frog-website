---
import Base from "@/layouts/Base.astro";
import { getCollection, render } from "astro:content";
import { FaGithub, FaLinkedin } from "react-icons/fa";
import { SiItchdotio } from "react-icons/si";

const team = (await getCollection("team")).sort(
  (a, b) => a.data.order - b.data.order,
);
---

<Base title="The Team" meta_title="The Team — You're a Frog?">
  <!-- Heading -->
  <section class="bg-gradient pt-40 pb-16 text-center">
    <div class="container">
      <h1 class="mb-4">Meet the Team!</h1>
      <p class="text-text-light text-lg">We are tinier alchemists, a team of 5 with a love for games. This game was developed for the Game Design course at KTH. Find out more about us below!</p>
    </div>
  </section>

  <!-- Team members -->
  <section class="section">
    <div class="container">
      <div class="flex flex-col gap-20">
        {
          team.map(async (member) => {
            const { Content } = await render(member);
            const { name, role, image, github, linkedin, itchio } = member.data;
            return (
              <div class="flex flex-col items-start gap-10 md:flex-row">
                <div class="h-90 w-full flex-shrink-0 overflow-hidden rounded-xl bg-border md:w-90">
                  <img
                    src={image}
                    alt={name}
                    class="h-full w-full object-cover"
                  />
                </div>
                <div class="flex-1">
                  <h2 class="mb-1">{name}</h2>
                  <p class="mb-5 text-sm uppercase tracking-widest text-text-light">
                    {role}
                  </p>
                  <div class="content text-lg leading-relaxed">
                    <Content />
                  </div>
                  <div class="mt-6 flex gap-4">
                    {github && (
                      <a href={github} target="_blank" rel="noopener" class="text-2xl text-text-light hover:text-primary transition">
                        <FaGithub />
                      </a>
                    )}
                    {linkedin && (
                      <a href={linkedin} target="_blank" rel="noopener" class="text-2xl text-text-light hover:text-primary transition">
                        <FaLinkedin />
                      </a>
                    )}
                    {itchio && (
                      <a href={itchio} target="_blank" rel="noopener" class="text-2xl text-text-light hover:text-primary transition">
                        <SiItchdotio />
                      </a>
                    )}
                  </div>
                </div>
              </div>
            );
          })
        }
      </div>
    </div>
  </section>
</Base>
